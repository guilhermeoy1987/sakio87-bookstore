name: Python Action Demo

on:
  push:
    branches: [actiondemo, actiondemo2]

jobs:
  qa:
    name: Quality check
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          # Criamos o usuário 'postgres' explicitamente para o Django não reclamar
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: bookstore_password
          POSTGRES_DB: bookstore_db
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install Poetry
        run: |
          curl -sSL https://install.python-poetry.org | python3 -
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          poetry install --no-interaction --no-root

      - name: Run Tests
        env:
          # O segredo: aqui dizemos ao Django para usar o usuário 'postgres' que agora existe
          SQL_ENGINE: django.db.backends.postgresql
          SQL_DATABASE: bookstore_db
          SQL_USER: postgres
          SQL_PASSWORD: bookstore_password
          SQL_HOST: 127.0.0.1
          SQL_PORT: 5432
          PGUSER: postgres
          PGPASSWORD: bookstore_password
        run: |
          poetry run python manage.py test
